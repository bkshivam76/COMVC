@using DevExtreme.AspNet.Mvc;
@(Html.DevExtreme().DataGrid().CustomDataGrid()
        .ID("CB_VouchedBy_List_DG")
        .Columns(columns =>
        {
            columns.Add().DataField("USER_ID").Caption("User");
            columns.Add().DataField("REC_ID").Caption("ItemID").Visible(false);
        })      
        .Selection(s => s.Mode(SelectionMode.Single))
   

)