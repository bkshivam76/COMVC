@using DevExtreme.AspNet.Mvc;
@(Html.DevExtreme().DataGrid().CustomDataGrid()
        .ID("CB_Zone_List_DG")
        .Columns(columns =>
        {
            columns.Add().DataField("Zone_Name").Caption("Zone");
        })

        .Selection(s => s.Mode(SelectionMode.Multiple))
        .SelectedRowKeys(new JS(@"component.option(""value"")"))

)