
@Html.DevExpress().GridView(
    settings =>
    {
        settings.Name = "AllowedVouchingCategoryListGrid";
        settings.CallbackRouteValues = new
        {
            Controller = "CashBookAuditor",
            Action = "Frm_Get_VouchingPreference_Grid",
        };
        settings.Settings.VerticalScrollBarMode = ScrollBarMode.Visible;
        settings.Settings.VerticalScrollableHeight = 200;
        settings.KeyboardSupport = true;
        //settings.AccessKey = "G";
        settings.Width = System.Web.UI.WebControls.Unit.Percentage(100);
        settings.SettingsAdaptivity.AdaptivityMode = GridViewAdaptivityMode.HideDataCells;
        settings.Settings.ShowGroupPanel = false;
        settings.SettingsBehavior.AutoExpandAllGroups = true;
        settings.SettingsBehavior.AllowSelectSingleRowOnly = true;
        settings.Settings.UseFixedTableLayout = true;
        settings.SettingsLoadingPanel.Mode = GridViewLoadingPanelMode.Default;
        settings.SettingsBehavior.AllowFocusedRow = true;
        settings.SettingsBehavior.AllowSelectByRowClick = false;
        settings.Settings.ShowHeaderFilterButton = true;
        settings.Settings.ShowFilterRow = false;
        settings.Settings.ShowHeaderFilterBlankItems = false;
        settings.Settings.AutoFilterCondition = AutoFilterCondition.Contains;
        settings.SettingsPager.Mode = GridViewPagerMode.ShowAllRecords;

        settings.SettingsContextMenu.Enabled = false;
        settings.SettingsBehavior.EnableCustomizationWindow = true;
        settings.SettingsContextMenu.RowMenuItemVisibility.DeleteRow = false;
        settings.SettingsContextMenu.RowMenuItemVisibility.EditRow = false;
        settings.SettingsContextMenu.RowMenuItemVisibility.NewRow = false;

        settings.ClientSideEvents.FocusedRowChanged = "AllowedVouchingCategory_FocusedRowChange";
        settings.Init = (s, e) =>
        {
            MVCxGridView grid = (MVCxGridView)s;
            grid.FocusedRowIndex = -1;
        };
        settings.CustomUnboundColumnData = (s, e) =>
        {
            if (e.Column.FieldName == "SR")
            {
                e.Value = e.ListSourceRowIndex + 1;
            };
        };
        settings.KeyFieldName = "Code";
        settings.Columns.Add(column =>
        {
            {
                column.FieldName = "SR";
                column.Caption = "Sr";
                column.UnboundType = DevExpress.Data.UnboundColumnType.Integer;
                column.MaxWidth = 30;

            }
        });

        settings.Columns.Add(column =>
        {
            {
                column.FieldName = "Category";
                column.Caption = "Category";

            }
        });
        settings.Columns.Add(column =>
        {
            {
                column.FieldName = "Code";
                column.Caption = "Code";
                column.Visible = false;


            }
        });
        MyExtensions.GridViewHelper.CustomGridView(settings);
    }).Bind(Model).GetHtml()
