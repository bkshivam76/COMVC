@{   var SessionGUID = Request.QueryString["SessionGUID"]; }
@Html.DevExpress().GridView(settings =>
{
    int ShowHorizontalBar = ViewBag.ShowHorizontalBar == null ? 0 : (int)ViewBag.ShowHorizontalBar;
    settings.Name = "RejectionGrid_" + ViewData["DonationID"];
    settings.SettingsDetail.MasterGridName = "DonRegisterListGrid";
    settings.CallbackRouteValues = new { Controller = "DonationRegister", Action = "Frm_Donation_Register_Rejection_Grid", DonationID = ViewData["DonationID"], SessionGUID= SessionGUID };
    settings.Width = Unit.Percentage(100);
    settings.Settings.ShowHeaderFilterButton = true;
    settings.SettingsContextMenu.Enabled = true;
    settings.SettingsContextMenu.RowMenuItemVisibility.DeleteRow = false;
    settings.SettingsContextMenu.RowMenuItemVisibility.EditRow = false;
    settings.SettingsContextMenu.RowMenuItemVisibility.NewRow = false;//Redmine Bug #131651 fixed

    settings.KeyFieldName = "pKey";
    settings.Columns.Add("DS_TR_ID").Visible = false;
    settings.Columns.Add("Reason_Of_Rejection").Caption = "Reason Of Rejection";
    settings.Columns.Add(column =>
    {
        {
            column.FieldName = "Rejected_On";
            column.Caption = "Rejected On";
            column.Visible = true;
            column.ColumnType = MVCxGridViewColumnType.DateEdit;
        }
    });
    if (ShowHorizontalBar == 1)
    {
        settings.Settings.HorizontalScrollBarMode = ScrollBarMode.Visible;
        settings.SettingsAdaptivity.AdaptivityMode = GridViewAdaptivityMode.Off;
        settings.SettingsResizing.ColumnResizeMode = ColumnResizeMode.Control;
        settings.SettingsResizing.Visualization = ResizingMode.Postponed;
    }
    else
    {
        settings.SettingsAdaptivity.AdaptivityMode = GridViewAdaptivityMode.HideDataCells;
        settings.Settings.HorizontalScrollBarMode = ScrollBarMode.Hidden;
        settings.SettingsResizing.ColumnResizeMode = ColumnResizeMode.Disabled;
        //settings.SettingsAdaptivity.HideDataCellsAtWindowInnerWidth = 500;
    }
    settings.ClientSideEvents.BeginCallback = "Donreg_RejectGrid_BeginCallBack";
    MyExtensions.GridViewHelper.CustomGridView(settings);
}).Bind(Model).GetHtml()


