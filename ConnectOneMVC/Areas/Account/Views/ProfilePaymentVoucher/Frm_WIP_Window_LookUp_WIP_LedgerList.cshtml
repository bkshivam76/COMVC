@using DevExtreme.AspNet.Mvc;
@(Html.DevExtreme().DataGrid().CustomDataGrid()
    .ID("Frm_WIP_Window_Ledger_LookUp_ItemList_datagrid")

    .Columns(columns =>
    {
        columns.Add().DataField("WIP_LED_ID").Caption("WIP_LED_ID").Visible(false);
        columns.Add().DataField("WIP_LEDGER").Caption("WIP LEDGER");
        columns.Add().DataField("Txn_Cr_ItemId").Caption("Txn_Cr_ItemId").Visible(false);
        columns.Add().DataField("WIP_LED_ID").Caption("WIP_LED_ID").Visible(false);
        columns.Add().DataField("Txn_Cr_ItemName").Caption("Txn_Cr_ItemName").Visible(false);
    })

    .Selection(s => s.Mode(SelectionMode.Single))
.OnSelectionChanged("function(selectedItems){ var keys = selectedItems.selectedRowKeys;var DDvalue=component.option('value');if(keys!=DDvalue){component.option('value', keys[0]);}component.close();}")

          )