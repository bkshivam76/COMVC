@using DevExtreme.AspNet.Mvc;
@model ConnectOneMVC.Areas.Facility.Models.Address_Book_ViewModel
<style>
</style>
<div class="row">
    <div class="col-sm-12 col-md-12 col-lg-12">
        <div class="row">
            <div class="col-sm-12 col-md-4 col-lg-4">
                @Html.Partial("Drop_Request", String.Format("Send Your Request to Madhuban for ( {0} ~ Contact)...", Model.TempActionMethod.ToString().Substring(1)))
            </div>
            <div class="col-sm-12 col-md-5 col-lg-5">
                <label style="color:red"><i class="fas fa-info-circle"></i> Red Fields are mandatory</label>
            </div>
            <div class="col-sm-12 col-md-3 col-lg-3">
                <div class="styleActionBtn">
                    @(Html.DevExtreme().Button().CustomSaveButton().ID("AddressEdit").Text(Model.TempActionMethod == "_Delete" ? "Delete" : "Save").FocusStateEnabled(true).UseSubmitBehavior(false).Visible(Model.TempActionMethod == "_View" ? false : true).OnClick("AB_SaveClick") )

                    @(Html.DevExtreme().Button().CustomCloseButton().ID("AddressCancel").Text(Model.TempActionMethod == "_View" ? "Close" : "Cancel").OnClick(@<text> function () { close_AddressBook(); } </text>) )
                </div>
            </div>
        </div>
    </div>
</div>