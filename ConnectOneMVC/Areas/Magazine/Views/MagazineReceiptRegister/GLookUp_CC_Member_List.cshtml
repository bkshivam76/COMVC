@using DevExtreme.AspNet.Mvc;
@(Html.DevExtreme().DataGrid()
        .ID("Frm_Magzine_GLookUp_CC_Member_List_datagrid")
        .DataSource(new JS(@"component.option(""dataSource"")"))
        .Columns(columns =>
        {

            columns.Add().DataField("MEM_ID").Caption("Mem. ID").Width(150);
            columns.Add().DataField("OLD_ID").Caption("Old Mem. ID").Width(290);
            columns.Add().DataField("ID").Caption("ID").Width(0).Visible(false);
})
        .HoverStateEnabled(true)
        .Paging(p => p.PageSize(100))
        .Scrolling(s => s.Mode(GridScrollingMode.Infinite))
        .Height(220)
        .Selection(s => s.Mode(SelectionMode.Single))
        .OnSelectionChanged(@<text>
            function(selectedItems) {
            var keys = selectedItems.selectedRowKeys;
            component.option("value", keys);
            }
        </text>)
)




