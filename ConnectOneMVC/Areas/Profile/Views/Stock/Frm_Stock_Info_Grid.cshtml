
@Html.DevExpress().GridView(
    settings =>
    {
        settings.Name = "StockProfileListGrid";
        settings.CallbackRouteValues = new
        {
            Controller = "Stock",
            Action = "Frm_Stock_Info_Grid",
            FilterVisible = false,
            SearchVisible = false,
            ShowGroupedColumns = false
        };
       //settings.Columns.Add("Store_Name").Caption = "Store_Name";
    }).Bind(Model).GetHtml()
