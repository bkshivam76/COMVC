@using DevExtreme.AspNet.Mvc;
@model System.Data.DataTable
<div>
    <label style="color:navy;font-size:15px"><b>@ViewBag.lblGradingHeader</b></label>
    </div>
<div id="GradingInfo_reloadPartialView" class="parent_table">
    <form>
        @Html.Partial("GradingInfo_Grid", Model)
    </form>
</div>
<script>
    function GradingInfoGrid_init(s, e)
    {
        GradingInfo_AdjustSize(s);
        ASPxClientUtils.AttachEventToElement(window, "resize", function (evt)
        {
            GradingInfo_AdjustSize(s);
        });
    }
    function GradingInfo_begincallback(s, e)
    {
        e.customArgs["command"] = e.command;
    }
    function GradingInfo_endcallback(s, e)
    {

    }
    function GradingInfo_AdjustSize(s)
    {
        var height = (document.documentElement.clientHeight * 90) / 100;
        try { s.SetHeight(height); } catch (err) { }
    }
    function GradingInfo_ContextMenuClick(s, e)
    {
        if (e.item.name == "Close")
        {
            CloseAuditInfo();
        }
        if (e.item.name == "ListPreview")
        {
            AuditInfoPreviewClick();
        }
        if (e.item.name == "Refresh")
        {
            GradingInfoListGrid.Refresh();
        }        
    }
    </script>