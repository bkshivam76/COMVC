@using DevExtreme.AspNet.Mvc;
@(Html.DevExtreme().DataGrid().CustomDataGrid()
                                    .ID("GetJobNameDG")
                              
                                    .Columns(columns =>
                                    {
                                        columns.Add().DataField("Job_Name").Caption("Job Name");
                                        columns.Add().DataField("Job_No").Caption("Job No.");
                                        columns.Add().DataField("Job_Type").Caption("Job Type");
                                        columns.Add().DataField("Complex").Caption("Complex Name");
                                        columns.Add().DataField("Proj_Name").Caption("Project Name");
                                        columns.Add().DataField("ID").Visible(false);

                
                        })
                                    .Selection(s => s.Mode(SelectionMode.Single))
                      )

