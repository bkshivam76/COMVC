@using DevExtreme.AspNet.Mvc;
@(Html.DevExtreme().DataGrid().CustomDataGrid()
                .ID("PurIOUnittDG")               
                .Columns(columns =>
                {
                    columns.Add().DataField("Unit").Caption("Unit Name");
                    columns.Add().DataField("Unit_ID").Caption("ID").Visible(false);


                })
              
                .Selection(s => s.Mode(SelectionMode.Single))
              
)


